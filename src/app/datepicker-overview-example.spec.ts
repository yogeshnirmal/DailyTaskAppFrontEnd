import { DatepickerOverviewExample } from './datepicker-overview-example';

describe('DatepickerOverviewExample', () => {
  it('should create an instance', () => {
    expect(new DatepickerOverviewExample()).toBeTruthy();
  });
});
